app:
  telegram-token: unset

spring:
  application:
    name: bot
  jackson:
    time-zone: UTC

springdoc:
  swagger-ui:
    path: /swagger-ui
server:
  port: 8090

scrapper:
  base-url: ${BASE_URL:http://localhost:8080}

logging:
  config: classpath:log4j2-plain.xml
